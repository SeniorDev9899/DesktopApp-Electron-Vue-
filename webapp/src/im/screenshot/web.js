const noop = $.noop;

export default {
    start: noop,
    captureScreen: noop,
    // params(callback: function)
    setShortcut: noop,
    setHideWindow: noop,
    // params(enabled:boolean)
    setEnabled: noop,
};
